<%= form_for(@firecar) do |f| %>
  <% if @firecar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@firecar.errors.count, "error") %> prohibited this firecar from being saved:</h2>

      <ul>
      <% @firecar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
